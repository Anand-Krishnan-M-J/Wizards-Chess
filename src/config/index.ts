export const squareSize = 50
export const offset = squareSize * 3.5

export const config = {
    square: {
        size: squareSize,
        xOffset: offset,
        yOffset: offset,
    },
}

export enum pieceColor {
    grey = 'grey',
    blackHover = '#989898',
    white = 'white',
    whiteHover = '#c6e5ee',

    blackSelect = '#a5a5a5',
    whiteSelect = '#b2dde8',
}
